### TODO ###

-> faire un niveau avec debut, fin de level, histoire, objectif

->	ajouter reticule

->	implémenter multithreading (probleme synchro à l'affichage, voir avec pthread_join)

->	implémenter inventaire
	permettre objet ramassable (gérer données)

->	implémenter un pistolet, munitions (gérer données)
	afficher la balle, et un trou comme impact du pistolet (nouveaux fichiers)

->	créer type de murs partiellement transparents afin de voir les espaces à l'arrière
	(nouveaux fichiers, gérer données)

->	sprites affichés sur les murs qui font tj face au joueur, et d'autre selon l'orientation (N, S, E, O)
	(nouveaux fichiers, gérer données)

->	comprendre comment faire texte à l'écran (sdl ttf) (nouveaux fichiers, gérer données)
	affichage de l'HUD de 3 éléments : inventaire d'une case, munitions, barre de pv
	link avec inventaire, link avec munitions, link avec pv, gérer texte

->	bouton sur un mur, actionnable, créer un event qui va avec (nouveaux fichiers, gérer données)

->	tous les éléments nécessaires doivent être dans un seul et unique fichier
	vous devez pouvoir lancer le jeu uniquement avec le binaire + ce fichier

->	skybox (pas juste une texture à la place du plafond, mais un "vrai ciel")

-> 	editeur de map :
	du t_env vers fichier .map

### TO CHECK ###

->	changer angle de vue haut/bas

->	luminosité différente dans une zone donnée

-> 	porte qui s'ouvre (animation à upgrade)

->	courir, se baisser, se relever

->	comprendre comment mettre de la musique
	gérer changement de musique

->	sol, plafond hauteur réglable / c'est fait mais...
	changer fonction d'affichage suivant hauteur?

->	textures sol + plafond (nouveaux fichiers)

->	sauter, tomber, voler : gérer gravité

### DONE ###
